# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# macOS
.DS_Store

# Secrets — never commit API keys or .env files
.env
*.key
secrets*

# Local prediction runs (keep only the curated ones under preds/)
preds_local_*.json
my_preds*.json
